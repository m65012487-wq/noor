# Atmosphere

Four quiet optional vector overlays: mist, light_rays, stars, clouds.
Transparent canvas 400×600, center anchor, cover. They have deliberately low opacity and no sparkle animation.
Not mounted by default: the current scene already has sufficient atmospheric tint. Enable only after checking contrast on reading and prayer screens. Do not add these to plain reading backgrounds.
